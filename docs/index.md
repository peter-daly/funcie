# funcie

`funcie` is a focused Python library of functional programming helpers.

## What it includes

- `identity(x)`: returns `x`
- `constant(v)`: returns a function that always returns `v`
- `predicate`: wraps boolean callables and supports logical composition with `&`, `|`, `^`, and `~`
